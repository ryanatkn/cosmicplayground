import {PortalBaseData} from '../portal.js';

const data: PortalBaseData = {
	title: 'deep breath',
	coolness: 7,
	showBackground: true, // TODO maybe hide by default but show when offmap areas are rendered?
	showBackButton: false,
};

export default data;
