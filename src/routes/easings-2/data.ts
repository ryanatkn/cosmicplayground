import type {PortalData} from '$lib/portal.js';
import Preview from '$routes/easings-2/Preview.svelte';

const data: PortalData = {
	name: 'easing function auralizations',
	slug: 'easings-2',
	showHomeButton: true,
	Preview,
};

export default data;
