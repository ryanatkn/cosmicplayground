import type {PortalData} from '$lib/app/portal';
import Preview from './Preview.svelte';

const data: PortalData = {
	name: 'freq spectacle',
	slug: 'freq-spectacle',
	showHomeButton: true,
	Preview,
};

export default data;
